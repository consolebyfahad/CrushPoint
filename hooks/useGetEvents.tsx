import React from "react";
import { StyleSheet, Text, View } from "react-native";

export default function useGetEvents() {
  return (
    <View>
      <Text>useGetEvents</Text>
    </View>
  );
}

const styles = StyleSheet.create({});
